import React from 'react';
import { connect } from 'react-redux';

const SidebarChat = () => {

    return (
        <div>
            SidebarChat
        </div>
    )

}

export default connect()(SidebarChat);